import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  server:{
  proxy: {
    '/api': {
      target: 'http://localhost:{{ backend_port }}',  // ✅ 后端地址
      changeOrigin: true,
    }
  },
  },
  build: {
  outDir: '../backend/static',      // ✅ 构建输出到后端
  emptyOutDir: true,
}
})
